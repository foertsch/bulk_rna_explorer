# Data files (too large for git) — but allow tiny toy datasets
data/*.rds
data/*.RDS
data/*.rds.gz
!data/toy_*.rds

# R session files
.Rhistory
.RData
.Ruserdata
.Rproj.user/

# R default graphics device dump (created when a script plots with no device)
Rplots.pdf

# Local editor / agent config
.claude/

# Stray local analysis artifacts (not part of this project)
/turnaround_waffle.png
/scripts/turnaround_waffle.R

# macOS
.DS_Store

# rsconnect deployment
rsconnect/
